# 卓望工作区执行规则

本文件适用于本仓库根目录及其全部子目录。

## 唯一正式知识源

- 本仓库根目录的 `卓望.md` 是卓望项目唯一正式版本（Single Source of Truth）。
- ChatGPT Library、Codex 本地目录、下载文件及其他位置的同名文件均只视为镜像或临时副本。
- 发生冲突时，优先级为：用户当前明确指令 > GitHub 最新版 `卓望.md` > 其他副本或历史记录。
- 不得在仓库内创建 `卓望(1).md`、`卓望(2).md`、`卓望-最新版.md` 等平行正式版本。

## 仓库文件分工

- `卓望.md` —— 稳定知识与规范（业务规则、省份本地化、交付偏好、原型规范、可复用经验）
- `进度.md` —— 易变的项目进度状态（各项目做到哪步、下一步是什么、谁最近经手）
- 两者不要混写：规范进 `卓望.md`，进度进 `进度.md`。

## 每次任务开始前

1. 先同步仓库最新状态；使用本地 Git 时优先执行 `git pull --ff-only`，存在未提交改动时不得强行覆盖。
2. 完整读取根目录 `卓望.md` 与 `进度.md` 后再分析、策划、写文案、制作原型或生成交付文件。
3. 从 `进度.md` 确认相关项目当前版本、状态与待确认事项，避免重复劳动或基于过时版本推进。
4. 优先应用其中已经确认的工作范围、业务规则、省份本地化要求、文档格式、原型规范及交付偏好。
5. 如果用户当前要求与档案冲突，以用户当前要求为准，并判断是否需要把新要求沉淀回 `卓望.md`。
6. 对方言、民俗、赛事、政策、活动时间、名单等可能变化或不确定的信息必须查证；无法确认时标记“待确认”，不得当作稳定事实写入档案。

## 重大任务完成后的自动复盘

以下任一情况视为重大任务：

- 完成一份活动策划、长 H5 原型、活动规则、客服文档、题库、拨测报告或数据复盘；
- 完成对既有方案的结构性修改；
- 用户确认了新的长期偏好、业务规则、页面机制、省份策略或交付规范；
- 发现可以在后续项目中复用的稳定流程、模板或避坑经验。

## 每次收工前

**无论任务大小，只要推进了某个项目，收工前必须更新 `进度.md` 对应条目**，写清当前版本、状态、下一步和最近经手，然后与其他改动一并提交推送。这样另外两个工具下次开工就能接上，不会重复劳动或基于过时版本推进。

只改本次实际推进的条目，不重写整个文件；条目按最近更新时间倒序排列。

## 重大任务完成后的自动复盘（续）

重大任务完成后必须：

1. 复盘本次任务，只提取稳定、已确认、可复用的信息。
2. 不把临时讨论、未确认猜测、一次性素材、账号凭据、个人敏感信息或冗长过程记录写入 `卓望.md`。
3. 将新增内容合并到 `卓望.md` 对应章节；优先修订原条目，避免重复堆叠。
4. 检查新内容是否与现有规则冲突；有冲突时保留最新已确认结论，并说明替代关系或适用范围。
5. 检查 Markdown 结构、标题层级、重复内容和敏感信息。
6. 若本次没有形成稳定新知识，不得为了“留下记录”而机械修改文件。
7. 有实际更新时，提交并推送到本仓库。推荐提交信息：`docs: update zhuowang knowledge`。
8. 推送后回读 GitHub 上的 `卓望.md`，确认内容与本地一致，再向用户说明新增或修订了哪些长期规则。

## 更新边界

- 不得自动删除用户已经确认的规则；需要废止时，应先确认或明确记录其已被新规则替代。
- 不得把其他公司、求职、生活、医疗、设备或无关项目的信息混入 `卓望.md`。
- 只把工具无关的业务知识（业务规则、玩法、文案规范、排版规范、活动策划结论等）写入 `卓望.md`；各工具专属的操作配置、技能与本地文件留在该工具本地目录，不进仓库，避免影响其他工具。
- 大段活动方案和交付成品应作为独立文件保存；`卓望.md` 只沉淀索引、规范、最终结论和可复用经验。
- 任何 GitHub 写入都只允许作用于用户明确授权的 `wangyucosmos/zhuowang-workspace` 仓库。

## 交付时的最低说明

任务结束时需明确说明：

- 本次交付物；
- 是否产生了值得沉淀的长期知识；
- 如果更新了 `卓望.md`，概述更新内容并给出对应提交；
- 如果没有更新，简要说明原因。
